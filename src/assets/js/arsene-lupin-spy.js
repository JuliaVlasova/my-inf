export function ArseneLupinSpy() {
  const lupin = document.getElementById('arsene-lupin')





}

/* Если с инфом не говорят больше 2000ms, то у него включается режим слежки за мышкой. 
Используй тригонометрические функции в CSS, чтобы создать плавный ступенчатый переход с задержкой
.character {
transition-delay:
calc(sin((var(--index) / 12) * 45deg) * 0.475s);
}
 */